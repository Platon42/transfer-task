/*
package mercy.digital.transfer.presentation.client;

import org.junit.Assert;
import org.junit.Before;
import org.junit.jupiter.api.Test;

import java.sql.Date;

import static org.junit.jupiter.api.Assertions.*;

class AddClientTest {

    @Before
    void initTest(){
        AddClient addClient = new AddClient();


    }


    @Test
    void setAddClientMember() {
        AddClient addClient = new AddClient();
        Date date = new Date(2019-10-10);
        String firstName = "Ivan";
        String lastName = "Petrov";
        String middle = "Oleksiyviechis";

        addClient.setBirthday(date);
        addClient.setFirstName(firstName);
        addClient.setLastName(lastName);
        addClient.setMiddleName(middle);


        Assert.assertEquals(date,addClient.getBirthday());
        Assert.assertEquals(firstName,addClient.getFirstName());

    }

}*/
